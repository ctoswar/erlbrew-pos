# Dependencies
node_modules/

# Build output
dist/

# Environment files (NEVER commit these — keep real values local only)
.env
.env.local
.env.production
server/.env
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
server/server.log
server/server.err

# TLS certificates and keys (NEVER commit — contain private keys)
server.pfx
server.pem
server.key
server.cer

# Scripts with hardcoded secrets
convert-cert.ps1
gen-cert.ps1

# Print server (keep on Pi, not in repo)
print-server.py

# Misc
*.tgz
server/src/migrate-pins.mjs
server/src/create-time.mjs
server/src/migrate-costing.mjs
